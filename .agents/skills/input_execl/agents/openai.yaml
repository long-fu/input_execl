interface:
  display_name: "Input Execl"
  short_description: "Repo-specific patterns and workflows for input_execl"
  default_prompt: "Use the input_execl repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true